import { SubscriptionDirective } from './subscription.directive';

describe('SubscriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new SubscriptionDirective();
    expect(directive).toBeTruthy();
  });
});
